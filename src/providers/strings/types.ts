export interface StringsContextType {
  search: {
    placeholder: string
  }
  password: {
    placeholder: string
    confirmationPlaceholder: string
    requirements: string
    digit: string
    uppercase: string
    match: string
  }
  email: {
    placeholder: string
  }
}
